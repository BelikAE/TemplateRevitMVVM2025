<Window x:Class="TemplateRevitMVVM2025.View.WindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateRevitMVVM2025.View"
        mc:Ignorable="d"
        Title="WindowView" Height="350" Width="600">
    <StackPanel>
        <Label Content="Тип стены"
            Margin="5"/>
        <ListBox ItemsSource="{Binding WallsType}"
           DisplayMemberPath="Name"
           MaxHeight="200"
           Margin="5"/>
        <Button Content="Найти типы стен"
            Command="{Binding FindWallTypeCommand}"/>
        <Button Content="Закрыть"
            Command="{Binding CloseCommand}"/>
    </StackPanel>
</Window>
